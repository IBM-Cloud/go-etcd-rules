// Package rules is a rules engine for etcd. Specified functions are triggered
// whenever a specified rule is satisfied.  Rules can contain gin-style attribute
// specifiers so that classes of keys are matched as opposed to having to specify
// each node key separately.
package rules
